box: nodesource/trusty

build:
  steps:
    - npm-install
    - script:
        name: SPA
        code: |
          ls node_modules/
          npm run build

deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: dist
