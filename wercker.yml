box: nodesource/trusty

build:
  steps:
    - script:
        name: SPA
        code: |
          npm install
          npm run build

deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: dist
