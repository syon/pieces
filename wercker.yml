box: nodesource/trusty

build:
  steps:
    - script:
        name: SPA
        code: |
          cd spa
          npm install
          npm run build

    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: spa/dist
