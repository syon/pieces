box: nodesource/trusty

build:
  steps:
    - npm install

    - script:
        name: SPA
        code: |
          npm run build

    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: spa/dist
